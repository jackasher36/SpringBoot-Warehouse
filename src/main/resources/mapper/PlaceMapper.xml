<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jackasher.ware_manager.mapper.PlaceMapper">

    <!--
      //查询所有产地
      public List<Place> findAllPlace()
    -->
    <select id="findAllPlace" resultType="com.jackasher.ware_manager.entity.Place">
        select * from place
    </select>

</mapper>